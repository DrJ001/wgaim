% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkMap.qtlAim.R
\name{linkMap.qtlAim}
\alias{linkMap.qtlAim}
\title{Plot a Linkage Map with QTL Overlay (\code{qtlAim})}
\usage{
\method{linkMap}{qtlAim}(
  object,
  ...,
  genObj,
  chr,
  chr.dist,
  marker.names = "markers",
  flanking = TRUE,
  qtl.colour = "steelblue",
  marker.colour = "firebrick",
  trait.colour = qtl.colour,
  m.cex = 7,
  trait.labels = NULL,
  tick = FALSE,
  nrow = 1,
  row.chr = NULL
)
}
\arguments{
\item{object}{A \code{qtlAim} object, or the first of several when
multiple models are passed for a multi-trait overlay.}

\item{\dots}{Additional \code{qtlAim} models (positional, unnamed) for
multi-trait display, and/or named arguments passed through to
\code{\link{linkMap.cross}}.}

\item{genObj}{The \code{"wgCross"} object produced by
\code{\link{primeCross}} used in the analysis.}

\item{chr}{Optional character vector of chromosome names to display.}

\item{chr.dist}{Optional named list with \code{$start} / \code{$end}
elements restricting the displayed cM range per chromosome.}

\item{marker.names}{\code{"markers"} (default), \code{"dist"},
\code{"none"}, or \code{NULL}.}

\item{flanking}{Logical. If \code{TRUE} (default), only the QTL flanking
markers are annotated on the right axis.}

\item{qtl.colour}{Colour(s) for QTL fills. Recycled across traits in
multi-model mode.}

\item{marker.colour}{Colour for flanking marker labels.}

\item{trait.colour}{Colour(s) for left-side trait labels. Defaults to
\code{qtl.colour}.}

\item{m.cex}{Text size (pt) for labels. Default \code{7}.}

\item{trait.labels}{Optional character vector of trait names (one per
model). Defaults to the left-hand side of each model's fixed formula.}

\item{tick}{Logical. Unused; retained for backward compatibility.}

\item{nrow}{Integer or \code{"auto"}. Number of facet rows. Default
\code{1}.}

\item{row.chr}{Optional named list of character vectors for manual
multi-row chromosome layout.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object.
}
\description{
Plots the genetic linkage map for a \code{"wgCross"} object with detected
QTL overlaid as coloured fills inside each chromosome bar. Flanking markers
are annotated on the right; trait labels appear on the left.

Multiple \code{qtlAim} models can be overlaid by passing them as extra
positional arguments, e.g.\
\code{linkMap(qtl_yld, qtl_tgw, genObj = my_int)}, producing a
multi-trait legend with automatic colour assignment.
}
\examples{
\dontrun{
# Single model
linkMap(qtl.fit, genObj = genoRxK)

# Multi-trait overlay
linkMap(qtl.yld, qtl.tgw, genObj = genoRxK,
        trait.labels = c("Yield", "TGW"))
}

}
\seealso{
\code{\link{linkMap}}, \code{\link{linkMap.cross}},
  \code{\link{linkMap.gwasAim}}, \code{\link{qtlAim}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.fineMap.R
\name{plot.fineMap}
\alias{plot.fineMap}
\title{Plot fine-mapping results}
\usage{
\method{plot}{fineMap}(
  x,
  col = "steelblue",
  peak.col = "firebrick",
  qtl.col = "navy",
  ref.col = qtl.col,
  ncol = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"fineMap"} produced by
\code{\link{fineMap}}.}

\item{col}{Line colour.  Default \code{"steelblue"}.}

\item{peak.col}{Colour for the fine-map peak point and label.  Default
\code{"firebrick"}.}

\item{qtl.col}{Colour for the original QTL reference point, label, and
vertical dashed line.  Default \code{"navy"}.}

\item{ref.col}{Colour of the vertical dashed reference line.  Defaults to
\code{qtl.col}.}

\item{ncol}{Number of columns in the \code{facet_wrap} layout.  Defaults
to \code{NULL}, which uses \code{min(n_qtl, 4)} columns (so up to four
QTL appear on one row; five or more wrap automatically).}

\item{\dots}{Currently ignored.}
}
\value{
A \code{ggplot} object (invisibly) with attribute
  \code{suggested_height} giving an appropriate plot height in inches.
  The plot is also printed.
}
\description{
Produces a ggplot2 LOD profile for each fine-mapped QTL / marker.  Each
QTL is shown in its own facet panel with two labelled reference points:
\describe{
  \item{Peak}{The grid position with the maximum LOD score, shown in
    \code{peak.col}.}
  \item{QTL}{The original QTL / marker position (centre of the fine-mapping
    window) as returned by \code{qtlAim} / \code{gwasAim}, shown in
    \code{qtl.col} with a vertical dashed reference line.  Suppressed when
    the peak coincides with the original position.}
}
Labels are placed with \code{ggrepel} to avoid overlap.
}
\seealso{
\code{\link{fineMap}}, \code{\link{print.fineMap}}
}

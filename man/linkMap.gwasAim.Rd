% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkMap.gwasAim.R
\name{linkMap.gwasAim}
\alias{linkMap.gwasAim}
\title{Plot a Linkage Map with Significant Marker Overlay (\code{gwasAim})}
\usage{
\method{linkMap}{gwasAim}(
  object,
  ...,
  genObj,
  chr,
  marker.names = "markers",
  marker.colour = "firebrick",
  label.colour = marker.colour,
  trait.labels = NULL,
  trait.colour = marker.colour,
  band.lwd = NULL,
  m.cex = 7,
  nrow = 1,
  row.chr = NULL
)
}
\arguments{
\item{object}{A \code{gwasAim} object, or the first of several when
multiple models are passed for a multi-trait overlay.}

\item{\dots}{Additional \code{gwasAim} models (positional, unnamed) for
multi-trait display, and/or named arguments passed through to
\code{\link{linkMap.cross}}.}

\item{genObj}{The \code{"wgPanel"} object produced by
\code{\link{primePanel}} used in the analysis.}

\item{chr}{Optional character vector of chromosome names to display.}

\item{marker.names}{\code{"markers"} (default), \code{"dist"},
\code{"none"}, or \code{NULL}.}

\item{marker.colour}{Colour for significant-marker bands (single-model
mode). Default \code{"firebrick"}.}

\item{label.colour}{Colour for right-side marker name labels. Defaults to
\code{marker.colour}.}

\item{trait.labels}{Optional character vector of trait names (one per
model). Defaults to the left-hand side of each model's fixed formula.}

\item{trait.colour}{Colour(s) for left-side trait labels and multi-model
bands. Defaults to \code{marker.colour}.}

\item{band.lwd}{Band linewidth in mm. Defaults to the internal
\code{BAR_FILL_LW} constant.}

\item{m.cex}{Text size (pt) for labels. Default \code{7}.}

\item{nrow}{Integer or \code{"auto"}. Number of facet rows. Default
\code{1}.}

\item{row.chr}{Optional named list of character vectors for manual
multi-row chromosome layout.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object.
}
\description{
Plots the genetic map for a \code{"wgPanel"} object with significant GWAS
markers overlaid as coloured horizontal bands inside each chromosome bar.
Marker names are annotated on the right; trait labels appear on the left.

Multiple \code{gwasAim} models can be overlaid by passing them as extra
positional arguments, e.g.\
\code{linkMap(gwas1, gwas2, genObj = my_panel)}, producing a multi-trait
legend with automatic \code{hcl.colors} colour assignment.
}
\examples{
\dontrun{
# Single model
linkMap(gwas.fit, genObj = panel)

# Multi-trait overlay
linkMap(gwas.yld, gwas.pro, genObj = panel,
        trait.labels = c("Yield", "Protein"))
}

}
\seealso{
\code{\link{linkMap}}, \code{\link{linkMap.cross}},
  \code{\link{linkMap.qtlAim}}, \code{\link{gwasAim}}
}

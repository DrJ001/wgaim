% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manhattan.R
\name{manhattan}
\alias{manhattan}
\alias{manhattan.GWASAim}
\title{Manhattan Plot for GWASAim Objects}
\usage{
manhattan(object, ...)

\method{manhattan}{GWASAim}(
  object,
  panelObj,
  iter = NULL,
  chr = NULL,
  pt.col = c("steelblue", "grey50"),
  sig.col = "red",
  pt.cex = 0.6,
  ...
)
}
\arguments{
\item{object}{A \code{GWASAim} object.}

\item{\dots}{Additional arguments passed to methods.}

\item{panelObj}{The \code{"panel"} object passed to \code{GWASAim}, used
to look up marker cM positions for the x-axis.}

\item{iter}{Integer vector of iterations to display. Default is all stored
iterations (\code{NULL}). Each iteration produces one facet panel.}

\item{chr}{Optional character vector of chromosome names to display. Default
is all chromosomes.}

\item{pt.col}{Character vector of length 2 giving alternating chromosome
point colours. Default is \code{c("steelblue","grey50")}.}

\item{sig.col}{Colour used to highlight and label the significant marker
in each facet. Default is \code{"red"}.}

\item{pt.cex}{Numeric point size. Default is \code{0.6}.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object. The y-axis shows the raw
  outlier statistic \eqn{\tilde{q}_i^2 / \tilde{v}_i} (not a transformed
  p-value). The selected marker in each iteration is shown as a larger
  coloured diamond with its marker name as a label.
}
\description{
Produces a genome-wide Manhattan plot of outlier statistics from a
\code{\link{GWASAim}} forward-selection analysis, with one facet per
requested iteration. Each facet shows the outlier statistic for all active
markers at that iteration, with the marker selected as significant at that
iteration highlighted and labelled. No significance threshold line is drawn:
significant markers have already been determined by the forward-selection
likelihood ratio test.
}
\section{Methods (by class)}{
\itemize{
\item \code{manhattan(GWASAim)}: Manhattan plot for a \code{GWASAim} object.

}}
